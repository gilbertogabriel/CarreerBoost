import 'package:carrier_boost/presentation/common/components/foo_app_bar.dart';
import 'package:flutter/material.dart';

class FreightOriginDestiny extends StatelessWidget {
  const FreightOriginDestiny({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: DefaultAppBar(titleAppBar: 'Calculadora de frete'),
      body: ListView(
        children: [

        ],
      ),
    );
  }
}
